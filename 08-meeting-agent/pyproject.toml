[project]
name = "meeting-agent"
version = "0.1.0"
description = "A LangGraph agent that plans meetings and updates GitHub issues from transcripts (breakout exercise: what to test?)"
requires-python = ">=3.12"
dependencies = ["langgraph>=1.2", "langchain-core>=1.6", "langchain-litellm>=0.8", "httpx>=0.28"]

[tool.uv]
package = false
